const db = require("../src/models");
const { Itinerary } = db;

const ItineraryService = {
  createOneItinerary: async () => {
    // params:
    /* 
    places = [data],
    accommodations = [],
    cuisine = []    
    */
    /* Get full trip  */
    /* 

    */
    /* POST */
    /* 
    
    forEach in mainPlaces ==> Itinerary.addPlace(place)
    forEach in mainAccomms ==> Itinerary.addAccommodation(accommodation)
    forEach in mainCuisine ==> Itinerary.addCuisine(cuisine)
    */
  },
};
module.exports = ItineraryService;
